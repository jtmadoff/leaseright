import React from "react";
import { createRoot } from "react-dom/client";
import LeaseRightApp from "./App.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <LeaseRightApp />
  </React.StrictMode>
);
